#include<stdio.h>
#include<string.h>

//(15) 3. Implement in C the function char * doubleup(const char * s)
//        which, given a string s, returns a new string consisting of s with each
//        character repeated twice. So doubleup("nabucco") is "nnaabbuuccccoo".
//
char* doubleup(const char* s);

int main(){
	//doubleup("nabucco");
	char* string="hello";
	printf("%s\n", string);
	printf("%d\n", strlen(string));
}

char* doubleup(const char* s){
	// set a new string, visit a single letter in the old string, put it in the new string twice.
	char* newstring;
	return newstring;
	while (*s){
		*
		s++;
	}
}
